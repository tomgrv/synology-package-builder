#!/bin/sh

# Same handling as a fresh install: the broker recreates the container from
# the approved snapshot, or asks for a new approval if the profile changed.
exec "$(dirname $0)"/postinst.docker "$@"

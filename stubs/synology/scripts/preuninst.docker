#!/bin/sh

# Keep the broker and the approval across upgrades: only a real uninstall
# revokes the elevation (container, sudoers entry and broker directory).
if [ "${SYNOPKG_PKG_STATUS:-}" = "UPGRADE" ]; then
    exit 0
fi

if [ -x "$BROKER" ]; then
    sudo -n "$BROKER" teardown
fi

exit 0
